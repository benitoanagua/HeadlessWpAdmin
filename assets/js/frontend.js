// Entry point para el frontend
console.log("Frontend JS cargado");

// Si usas Svelte, importa tu componente principal
// import App from '../svelte/FrontendApp.svelte';

// Si necesitas inicializar algo
document.addEventListener("DOMContentLoaded", function () {
  console.log("DOM cargado - frontend");
});
