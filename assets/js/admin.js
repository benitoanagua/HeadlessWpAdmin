// Entry point para el admin
console.log("Admin JS cargado");

// Si usas Svelte, importa tu componente principal
// import App from '../svelte/AdminApp.svelte';

// Si necesitas inicializar algo
document.addEventListener("DOMContentLoaded", function () {
  console.log("DOM cargado - admin");
});
